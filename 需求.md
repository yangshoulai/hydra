# Hydra (九头蛇) - 高可用大模型聚合网关产品需求文档

## 1. 项目背景与痛点 (Background & Pain Points)

### 1.1 背景

开发者和中小企业在接入大模型服务时，常混合使用官方 API 和第三方低成本渠道（俗称“公益站”或“中转站”）。这些渠道虽然便宜，但普遍存在服务不稳定的情况。

### 1.2 核心痛点 (Why we build this)

1. **单点故障误杀（粗颗粒度管理）**：
2. - 现有主流工具（如 OneAPI/NewAPI）通常以“渠道”为最小存活单位。
   - 一旦渠道内某一个 Key 失效（如余额不足），或者某一个模型（如 gpt-4）报错，整个渠道（包含其他正常的 Key 和模型）往往会被整组禁用，导致可用资源被严重浪费。
3. **软失败（Soft Failures / Fake 200）**：
   - 上游接口经常返回 HTTP 200 状态码，但响应 Body 却是“无可用后端”、“额度不足”等报错文本或 HTML 网页。
   - 传统网关无法识别此类错误，直接透传给客户端，导致应用端解析失败，或用户看到乱码/报错信息，体验极差。
4. **模型维护成本高**：
   - 不同上游渠道对同一个模型（如 `gpt-4`）的命名千奇百怪（如 `vip-gpt4-0613`, `gpt-4-fast`）。
   - 上游模型经常变动（新增/下架），管理员难以手动一一维护映射关系，且难以发现配置早已失效。

### 1.3 项目目标 (Hydra 理念)

构建一个**“砍掉一个头，长出两个头”**的高韧性网关。

- **细粒度熔断**：Key 坏了封 Key，模型坏了封模型，绝不轻易误杀整个渠道。
- **智能清洗**：识别并拦截“假 200”响应，内部自动重试，对用户屏蔽上游故障。
- **自动化运维**：提供模型自动发现与差异比对功能，降低维护成本。

## 2. 核心功能需求 (Functional Requirements)

### 2.1 代理与路由 (Proxy & Routing)

- **多协议支持**：
  - `POST /v1/chat/completions` (核心业务接口，支持 SSE 流式)。
  - `GET /v1/models` (虚拟列表接口，返回系统配置的统一模型名，欺骗客户端校验)。
- **智能路由策略**：
  - **统一命名**：对外暴露标准名（如 `gpt-4`），请求时自动重写为上游真实名（如 `vip-gpt4`）。
  - **负载均衡**：支持权重（Weight）和 优先级（Priority）分配流量。
  - **Key 池化管理**：单渠道下支持配置海量 Key，系统自动轮询使用。

### 2.2 熔断与韧性 (Resilience & Circuit Breaker)

系统需维护 **Key** 和 **Channel** 两种维度的健康状态，实现**“错误分级处理”**。

| 错误类型   | 典型场景                                 | 处理动作                                                 | 恢复策略                                      |
| :--------- | :--------------------------------------- | :------------------------------------------------------- | :-------------------------------------------- |
| **硬故障** | 401, 402, 429 (quota), 403               | **Key 级死刑**：永久禁用该 Key，立即换 Key 重试。        | 人工重置                                      |
| **软故障** | 5xx, Timeout, 网络错误, 429 (rate limit) | **Key/Channel 级熔断**：连续失败 N 次后，进入“冷却中”。  | 冷却时间结束后，允许 1 个请求试探 (Half-Open) |
| **假死**   | 200 OK 但 Body 含 `error`/`maintenance`  | **拦截重试**：不返回给用户，视为软故障处理，自动换渠道。 | 同上                                          |

### 2.3 模型同步与漂移检测 (Sync & Diff)

- **发现**：利用渠道 Key 实时调用上游 `/v1/models` 接口。
- **比对**：将上游实时列表与本地数据库配置比对，计算出：
  - 🟢 **新增 (New)**：上游新上架的模型。
  - 🔴 **缺失 (Missing)**：本地配置有，但上游已下架。
  - 🔵 **存量 (Existing)**：两边都有（保留本地的重命名配置）。
- **应用**：用户在 UI 上查看差异视图，一键处理变更。

### 2.4 全链路日志系统 (Logging System)

- **双层日志架构**：
  - **审计日志 (DB)**：记录 TraceID、用户、模型、耗时、状态码、简略报错。支持在 UI 上按条件快速检索。
  - **调试日志 (File)**：记录 TraceID、完整 Request/Response Body（仅出错或 Debug 模式记录）。用于开发人员通过 TraceID 去服务器排查“疑难杂症”。
- **生命周期管理**：
  - 支持配置日志保留天数（如 30 天）。
  - 后台任务每日自动物理删除过期 DB 日志，并执行 SQLite `VACUUM` 释放空间。
  - 文件日志自动轮转切割（Log Rotation）。

---

## 3. UI/UX 设计规划 (Admin Dashboard)

**技术栈**：Vue 3 + Naive UI (SPA)

### 3.1 仪表盘 (Dashboard)

- **核心指标**：QPS 实时波形图、今日请求成功率。
- **健康墙**：可视化展示所有渠道的实时状态（正常/熔断/禁用）。

### 3.2 渠道管理 (Channel Manager)

- **列表页**：展示渠道基础信息及 Key 池健康度（如 `Keys: 8/10`，表示 2 个已废）。
- **编辑/新建向导**：
  - **基础配置**：名称、BaseURL、Key 池录入。
  - **模型配置 (Diff View)**：
    - 点击 **[🔄 同步模型]**。
    - 展示**差异对比表格**（新增的高亮，缺失的标红）。
    - 用户确认映射关系及权重后保存。
- **测试工具**：[⚡ 立即测活] 按钮，对该渠道所有 Key 发起并发轻量检测。

### 3.3 日志查询 (Log Viewer)

- **筛选器**：时间范围、TraceID、状态（成功/失败）、渠道名称。
- **列表**：展示摘要信息。
- **详情抽屉**：展示请求元数据、错误信息，并提供“复制 TraceID”功能以便查找文件日志。

### 3.4 系统设置 (Settings)

- **策略配置**：调整连续失败阈值、冷却时长、探测策略。
- **维护配置**：设置日志保留时长、手动触发日志清理。
- **访问控制**：管理访问 Hydra 的 Token。

---

## 4. 技术栈选型 (Tech Stack)

| 模块          | 选型                      | 理由                                                        |
| :------------ | :------------------------ | :---------------------------------------------------------- |
| **语言**      | **Go (1.21+)**            | 高并发优势，处理 HTTP SSE 流式转发性能极佳，部署简单。      |
| **Web 框架**  | **Gin**                   | 轻量级、高性能、中间件生态成熟。                            |
| **数据库**    | **SQLite (默认)**         | 零配置，单文件存储，适合私有化部署。GORM 支持扩展至 MySQL。 |
| **ORM**       | **GORM**                  | 也就是操作数据库的工具，开发效率高。                        |
| **Migration** | **Gormigrate**            | 代码化数据库版本管理，支持系统平滑升级。                    |
| **前端**      | **Vue 3 + Naive UI**      | 组件丰富（特别是数据表格），TypeScript 支持好。             |
| **构建**      | **Go Embed**              | 前端构建产物直接打包进 Go 二进制文件，实现“单文件分发”。    |
| **日志**      | **Slog** + **Lumberjack** | 结构化日志 + 文件自动轮转切割。                             |

---

## 5. 数据库设计 (Schema Overview)

### 5.1 `channels`

渠道基础表。

- `id`, `name`, `base_url`, `status` (Enabled/Disabled/Cooling), `priority`, `weight`.

### 5.2 `keys`

密钥池表，归属于 Channel。

- `id`, `channel_id`, `content` (sk-xxx), `status` (Active/Dead/Cooling), `fail_count`.

### 5.3 `channel_model_configs` (Mapping)

定义该渠道支持的模型及重定向规则。

- `id`, `channel_id`, `original_name` (上游名), `mapped_name` (统一名), `enabled`.

### 5.4 `request_logs`

审计日志表。

- `id`, `trace_id` (Index), `req_model`, `real_model`, `channel_id`, `status_code`, `duration_ms`, `error_msg` (Text), `created_at`.

### 5.5 `system_settings`

系统配置表（KV 存储）。

- `key`: `log_retention_days`, `value`: `30`
- `key`: `circuit_breaker_threshold`, `value`: `3`

---

## 6. 开发规划与里程碑 (Roadmap)

### 阶段一：MVP 核心 (预计 2 周)

- **目标**：跑通 `Client -> Hydra -> Upstream` 的全流程，不含复杂 UI。
- **任务**：
  1. 初始化 Go 项目，集成 GORM + SQLite + Gormigrate。
  2. 实现 `/v1/chat/completions` 的参数解析、Header 替换、Body 替换。
  3. 实现基础的“轮询”路由与流式转发。
  4. 实现“审计日志写入 DB”的中间件。

### 阶段二：高可用与管理 (预计 2 周)

- **目标**：具备“自我修复”能力，且可以通过 UI 管理。
- **任务**：
  1. 实现 **Key Pool** 逻辑与状态机（失败计数、冷却、死刑）。
  2. 实现 **响应嗅探 (Sniffer)**：拦截 200 OK 中的软失败。
  3. 开发 **前端 UI**：渠道列表、新增/编辑页面。
  4. 实现后端 `/v1/models` 同步与 **Diff 比对算法**。
  5. 集成前端静态资源到 Go Binary。

### 阶段三：完善与交付 (预计 1 周)

- **目标**：生产环境就绪。
- **任务**：
  1. 实现 **日志自动清理** 后台任务。
  2. 完善 **仪表盘** 图表展示。
  3. 添加 **访问令牌 (Access Token)** 管理功能。
  4. 编写 Dockerfile 与 `docker-compose.yml`。
  5. 进行压力测试。

---

## 7. 附录：项目目录结构

```text
hydra/
├── cmd/
│   └── hydra/
│       └── main.go           # 程序入口
├── internal/
│   ├── model/                # GORM 实体定义
│   ├── db/                   # 数据库连接与 Migration
│   ├── proxy/                # 核心代理 (Handler, Sniffer, Rewriter)
│   ├── router/               # 路由算法 (Dispatcher, CircuitBreaker)
│   ├── service/              # 业务逻辑 (ChannelService, LogCleaner)
│   ├── api/                  # 管理后台 API
│   └── middleware/           # 鉴权、日志中间件
├── web/                      # 前端 Vue 源码
├── pkg/
│   ├── logger/               # 日志封装
│   └── utils/                # 通用工具
├── config.yaml               # 初始配置
├── Dockerfile
└── go.mod
```
